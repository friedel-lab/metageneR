% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{get_metagene_profiles}
\alias{get_metagene_profiles}
\title{Extract metagene profiles as a data.frame}
\usage{
get_metagene_profiles(
  run_dir = NULL,
  metagene_data = NULL,
  condition_mapper = NULL,
  aggr_rep_FUN = mean,
  ids_to_subset = NULL,
  gsub_name = NULL,
  threads = 1,
  overwrite_rds_objects = FALSE,
  cov_files_to_ignore = ".norm.coverage.csv",
  normByShapeSum = FALSE,
  normByShapeMax = FALSE,
  aggregateFun = mean,
  format = c("wide", "long")
)
}
\arguments{
\item{run_dir}{The run folder where binning took place. Provide either
\code{run_dir} or \code{metagene_data}, not both.}

\item{metagene_data}{Output of \code{\link{load_metagene_data}}. Provide either
this or \code{run_dir}.}

\item{condition_mapper}{See \code{\link{plot_metagene_experiment}}.
Ignored when \code{metagene_data} is provided.}

\item{aggr_rep_FUN}{Function to aggregate replicates. Default \code{mean}.
Ignored when \code{metagene_data} is provided.}

\item{ids_to_subset}{Character vector of IDs to filter regions.
Ignored when \code{metagene_data} is provided.}

\item{gsub_name}{Character string to remove from filenames.
Ignored when \code{metagene_data} is provided.}

\item{threads}{Number of threads. Ignored when \code{metagene_data} is provided.}

\item{overwrite_rds_objects}{Logical. Ignored when \code{metagene_data} is provided.}

\item{cov_files_to_ignore}{Regex to exclude files. Ignored when \code{metagene_data} is provided.}

\item{normByShapeSum}{Logical. Normalize by shape sum. Default \code{FALSE}.}

\item{normByShapeMax}{Logical. Normalize by shape max. Default \code{FALSE}.}

\item{aggregateFun}{Function to aggregate across features per bin. Default \code{mean}.}

\item{format}{Output format: \code{"wide"} (default, one column per condition)
or \code{"long"} (columns: condition, bin, value).}
}
\value{
A data.frame. Wide: columns \code{bin}, plus one per condition.
Long: columns \code{condition}, \code{bin}, \code{value}.
}
\description{
Returns the aggregated per-bin signal profiles that would be plotted
by \code{\link{plot_metagene_experiment}}, as a data.frame for downstream
analysis or custom plotting (e.g. with ggplot2).
}
