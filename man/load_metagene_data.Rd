% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{load_metagene_data}
\alias{load_metagene_data}
\title{Load metagene data from a run directory}
\usage{
load_metagene_data(
  run_dir,
  condition_mapper = NULL,
  aggr_rep_FUN = mean,
  ids_to_subset = NULL,
  gsub_name = NULL,
  threads = 1,
  overwrite_rds_objects = FALSE,
  cov_files_to_ignore = ".norm.coverage.csv"
)
}
\arguments{
\item{run_dir}{The run folder where binning took place.
Must contain a \code{config.json} file.}

\item{condition_mapper}{A data frame with columns \code{grep_name}, \code{display_name},
and optionally \code{color} and \code{linetype}. If \code{NULL} (default),
conditions are auto-detected from coverage files or a cached RDS.}

\item{aggr_rep_FUN}{Function to aggregate replicates. Default \code{mean}.}

\item{ids_to_subset}{Character vector of IDs to filter regions.}

\item{gsub_name}{Character string to remove from filenames for cleaner names.}

\item{threads}{Number of threads for parallel processing.}

\item{overwrite_rds_objects}{Logical. Overwrite cached RDS? Default \code{FALSE}.}

\item{cov_files_to_ignore}{Regex to exclude coverage files. Default \code{".norm.coverage.csv"}.}
}
\value{
A named list of aggregated binMatrix objects (one per condition).
Can be passed as \code{metagene_data} to \code{plot_metagene_experiment}
or \code{get_metagene_profiles}.
}
\description{
Reads binning results from a run directory, groups replicates by condition,
and returns the aggregated data. The result can be passed to
\code{\link{plot_metagene_experiment}} or \code{\link{get_metagene_profiles}}
to avoid re-loading from disk.
}
