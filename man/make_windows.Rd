% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_windows.R
\name{make_windows}
\alias{make_windows}
\alias{filter_gtf_by}
\title{Create genomic regions with upstream-downstream sizes and optional filters.}
\usage{
make_windows(
  input_file,
  upstream = 0,
  downstream = 0,
  path_to_output = NULL,
  position = NULL,
  save_pars_to = NULL,
  filter = filter_gtf_by()
)

filter_gtf_by(...)
}
\arguments{
\item{input_file}{GTF/GFF or BED file}

\item{upstream}{Upstream size}

\item{downstream}{Downstream size}

\item{path_to_output}{File path to write output. Creates dirs if they do not exist}

\item{position}{A character string ('start' or 'end') to specify
if the new region should be extended around only one coordinate.
Default is \code{NULL}.}

\item{save_pars_to}{A character string for the file to save the parameters.
Default is a "config.json" file in the same directory as the output.}

\item{filter}{Filtering expressions generated by filter_gtf_by().
Used to subset GTF entries before window generation.
Defaults to \code{filter_gtf_by()} (no filtering).}

\item{...}{Unquoted logical expressions to filter the GTF data. Use commas
to separate multiple conditions (which are joined with an "AND" logic).}
}
\value{
Invisibly a data frame in BED format with specified window and filter criteria.
Columns are "seqnames", "start", "end", "name", "score", "strand".
'seqnames' and 'strand' are factors.
As a side effect, it writes the data frame to \code{path_to_output} as a TSV file
if specified.

A list of language objects (calls) representing the filter expressions.
NULL if called empty (\code{filter_gtf_by()}).
}
\description{
Create genomic regions with upstream-downstream sizes and optional filters.
}
\details{
make_windows extends the annotations in the input file by the given upstream
and downstream regions, depending on the strand: A gene's start on the
minus strand is its end coordinate.
It is also possible to focus on a single coordinate by specifying \code{position}.
A region is built around the given coordinate, from \code{upstream}
bases back to \code{downstream} bases forward if it is on the plus strand,
and vice versa for the minus strand.

If GTF is provided, the \code{name} column is populated based on feature type:
\itemize{
\item \strong{gene/aggregate_gene}: Uses \code{gene_id}.
\item \strong{transcript}: Combines \code{gene_id} and \code{transcript_id}.
\item \strong{exonic_part}: Combines \code{gene_id} and \code{exonic_part_number}.
\item \strong{exon}: Combines \code{gene_id}, \code{transcript_id}, and \code{exon_number}.
}

If using \code{filter}, an error is thrown if used with a BED file or an incompatible format.

To filter entries from a GTF/GFF file, use logical operators
(\code{==}, \code{\%in\%}, \code{>}, etc.) using the column names of the annotation object.
Comma-separated expressions are reduced by concatenating them with "AND".
The function uses non-standard evaluation and applies the filters in a data.table environment.
This data.table is the object created by \code{rtracklayer::import()} from the provided annotation file,
which should contain columns like \code{seqnames}, \code{start}, \code{end}, \code{strand}, \code{source}, \code{type}, \code{gene_id}, \code{transcript_id}, etc.
Here are some examples of GTF variables one can use to filter:
seqnames, start, end, width, strand, source, type, score, phase, gene_id,
gene_version, gene_name, gene_source, gene_biotype, transcript_id, transcript_version,
transcript_name, transcript_source, transcript_biotype, tag, transcript_support_level,
exon_number, exon_id, exon_version, protein_id, protein_version, ccds_id...

\strong{Logical Operations:}
\itemize{
\item \strong{AND:} Separate expressions with commas (or use \code{&}): \verb{type == "exon", gene_name == "GAPDH"}.
\item \strong{OR:} Use the \code{|} operator within a single expression: \code{gene_name == "GAPDH" | gene_name == "ACTB"}.
\item \strong{Multiple Values:} To match ANY value in a set, use \code{\%in\%}: \code{type \%in\% c("gene", "exon")}.
}
}
\note{
Do not use \code{&&} or \code{||} as these are not vectorized and will only
evaluate the first row of the data.
}
\examples{
example_file <- system.file("extdata", "data", "example.gtf", package = "metageneR")
result <- make_windows(input_file=example_file,
                       upstream = 1000, downstream = 2000,
                       position = "start",
                       filter = filter_gtf_by())
print(head(result))
filter_gtf_by(type == "gene")
filter_gtf_by(type == "exon", source == "HAVANA")
filter_gtf_by(gene_name \%in\% c("GAPDH", "ACTB") | type == "transcript")
}
